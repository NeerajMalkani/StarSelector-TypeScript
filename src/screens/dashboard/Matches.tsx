import { View } from "react-native";
import { Text } from "react-native-paper";

const Matches = () => {
  return (
    <View>
      <Text variant="bodyLarge">Matches</Text>
    </View>
  );
};

export default Matches;
