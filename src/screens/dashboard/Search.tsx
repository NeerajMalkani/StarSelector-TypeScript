import { View } from "react-native";
import { BasicProps } from "../../models/Props";
import { Styles } from "../../styles/styles";

const Search = ({ theme }: BasicProps) => {
  return <View style={[Styles.flex1, { paddingBottom: 104 }]}></View>;
};

export default Search;
