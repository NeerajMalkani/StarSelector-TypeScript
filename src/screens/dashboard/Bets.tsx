import { View } from "react-native";
import { Text } from "react-native-paper";

const Bets = () => {
  return (
    <View>
      <Text variant="bodyLarge">Bets</Text>
    </View>
  );
};

export default Bets;
