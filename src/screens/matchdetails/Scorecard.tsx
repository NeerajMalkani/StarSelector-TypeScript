import { View } from "react-native";
import { Text } from "react-native-paper";

const Scorecard = ({ matchID }: any) => {
  return (
    <View>
      <Text variant="bodyLarge">Scorecard</Text>
    </View>
  );
};

export default Scorecard;
