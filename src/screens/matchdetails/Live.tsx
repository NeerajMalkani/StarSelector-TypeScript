import { View } from "react-native";
import { Text } from "react-native-paper";

const Live = ({ matchID }: any) => {
  return (
    <View>
      <Text variant="bodyLarge">Live</Text>
    </View>
  );
};

export default Live;
